var TextureLoader = new THREE.TextureLoader()

var Materials = {
    //normal: new THREE.MeshBasicMaterial({ side: THREE.DoubleSide, color: TileColor }),
    //star: new THREE.MeshBasicMaterial({ side: THREE.DoubleSide, color: TileColor, map: new THREE.TextureLoader().load('gfx/star.png') })
}

var TileGen = {
    color: "green",
    type: "board"
}